<nav class="home">
<%= link_to new_list_path do %><div class="new_list_button">+</div><% end %>
  <% if @lists.empty? %>
    <%="no list to display"%>
    <% else %>
  <% @lists.each do |i| %>
  <%=  link_to list_path(i.id) do %>
    <div class="list_item">
      <div class="list_item_bg"style= "background-image: url('<%= i.photo.attached? ? cl_image_path(i.photo.key) : image_path('pexels-obregonia-d-toretto-918281.jpg') %>')">
      </div>
      <div class="list_item_content"><%= i.name %></div>
      <% end %>
    </div>
  <% end %>
<% end %>
